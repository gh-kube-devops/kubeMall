spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/kubemall?currentSchema=user_schema
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: user_schema
    show-sql: false
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
      continue-on-error: true

logging:
  file:
    path: /var/log/mall-user
    name: /var/log/mall-user/mall-user.log