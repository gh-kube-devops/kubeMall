spring:
  application:
    name: mall-user

  datasource:
    url: jdbc:postgresql://localhost:5432/kubemall
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
server:
  port: 8080
